# Server Configuration
PORT=8080

# Database Configuration
# SQLite file path (will be created if it doesn't exist)
DB_PATH=./shopease.db

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here
